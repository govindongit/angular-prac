import { HighlightedCompletedTodoDirective } from './highlighted-completed-todo.directive';

describe('HighlightedCompletedTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightedCompletedTodoDirective();
    expect(directive).toBeTruthy();
  });
});
